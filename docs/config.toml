# The URL the site will be built for
base_url = "/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false
theme = "juice"

# Configuration of the Markdown rendering
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "inspired-github"

[extra]
# Put all your custom variables here
juice_logo_name = "K8S Search Extension"
juice_logo_path = "logo.svg"
juice_extra_menu = [
    { title = "Geddle", link = "https://geddle.com" },
    { title = "Github", link = "https://github.com/huhu/k8s-search-extension" },
]
meta_title = "K8S Search Extension"
meta_description = "The ultimate search extension for Kubernetes"
